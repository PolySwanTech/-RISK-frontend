import { Routes } from '@angular/router';
import { ControlChartComponent } from './control-chart/control-chart/control-chart.component';

export const controlRoutes: Routes = [
  { path: 'chart', component: ControlChartComponent }
];
